{
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "example"
    ]
  },
  "scripts": {
    "example": "yarn workspace @nevinha-code/example",
    "nc-highlight": "yarn workspace @nevinha-code/highlight",
    "nc-theme": "yarn workspace @nevinha-code/theme",
    "nc-build": "yarn workspace @nevinha-code/build",
    "watch-packages": "yarn nc-highlight watch && yarn nc-theme watch",
    "build-packages": "yarn nc-highlight build && yarn nc-theme build"
  }
}
